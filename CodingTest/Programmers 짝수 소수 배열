class Solution {
    public int[] solution(int[] num_list) {
        int[] answer = new int[2];

        for(int i = 0; i < num_list.length; i++)
            answer[num_list[i] % 2]++;

        return answer;
    }
}

/*
짝수, 소수를 저장하는 크기 2짜리 배열 answer
num_list[i]%2를 인덱스로 사용 가능 (0 또는 1만 나오므로)
*/
